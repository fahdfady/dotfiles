# Toggle all microphone inputs (not just the default one).
# Uses a chord to avoid conflicting with existing XF86AudioMicMute bindings.
bindld = Super+Shift+Alt, M, Toggle all microphones, exec, ~/.config/hypr/scripts/toggle-all-mics.sh

